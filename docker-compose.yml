version: '3.8'
services:
  postgres:
    image: postgres
    restart: always
    environment:
      - POSTGRES_USER=postgres
      - POSTGRES_PASSWORD=postgres
      - POSTGRES_DB=pdhours
    ports:
      - '5432:5432'
    volumes:
      - ./db:/docker-entrypoint-initdb.d/